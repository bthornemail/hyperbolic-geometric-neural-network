# H²GNN MCP Server Configuration

# Heartbeat interval in milliseconds (default: 30000 = 30 seconds)
# Set to 0 to disable heartbeat logging
H2GNN_HEARTBEAT_INTERVAL=30000

# Sync frequency in milliseconds (default: 300000 = 5 minutes)
# Set to 0 to disable periodic sync
H2GNN_SYNC_FREQUENCY=300000

# Storage path for persistence (default: ./persistence)
H2GNN_STORAGE_PATH=./persistence

# Shared learning database path (default: ./shared-learning)
H2GNN_SHARED_LEARNING_PATH=./shared-learning

# Enable/disable debug logging (default: false)
H2GNN_DEBUG=false

# Network mode for HD addressing (private, protected, public)
H2GNN_NETWORK_MODE=private

# HD addressing seed (optional, will generate if not provided)
# H2GNN_HD_SEED=your-seed-here

